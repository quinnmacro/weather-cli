name: CI

on: [push, pull_request]

jobs:
  ci:
    uses: quinnmacro/code-maintenance/.github/workflows/python-ci.yml@main
    with:
      python-version: '3.11'
      lint-blocking: false
      lint-scope: 'src/'
      install-command: 'pip install -e ".[dev]" || pip install -e .'
