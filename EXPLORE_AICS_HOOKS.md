# AICS Hook 系统能力边界探索指南

## 🆕 新功能：智能自动启用

**🎯 实现目标**: "一个项目一旦有了`.claude`目录，就自动启用AICS系统"

### ✅ 核心特性
1. **自动检测**: 如果项目目录有`.claude`子目录，AICS自动启用
2. **环境变量优先**: `AICS_HOOK_ENABLED=true/false` 覆盖自动检测
3. **调试输出**: 设置 `AICS_HOOK_DEBUG=true` 查看检测日志

### 🚀 使用方式
```bash
# 1. 安装AICS包
cd ~/Code/aics
pip install -e .

# 2. 启用全局配置（一次性）
cp ~/.claude/hooks-aics.json ~/.claude/hooks.json

# 3. 在任何项目中启用AICS
cd /path/to/your/project
mkdir .claude  # 创建这个目录即可自动启用

# 4. 开始对话 - 所有对话自动保存到 .aics/ 目录
```

## 项目概述

这是一个简单的Weather CLI项目，专门设计用于测试AICS Hook系统的完整能力范围。通过这个项目，我们将探索：

1. **基本功能** - 对话自动保存、项目检测
2. **高级功能** - 存储适配器、搜索、统计
3. **边界条件** - 长时间会话、大文件、复杂项目结构
4. **极限测试** - 并发、错误处理、性能

## 测试环境设置

### 1. 安装AICS Hook系统
```bash
# 切换到AICS项目目录
cd /Users/liulu/Code/aics
pip install -e .

# 启用Hook配置
cp ~/.claude/hooks-aics.json ~/.claude/hooks.json
```

### 2. 准备测试项目
```bash
cd /tmp/weather-cli

# 创建.claude目录启用AICS（新功能！）
mkdir -p .claude

# 可选：启用调试模式
export AICS_HOOK_DEBUG=true

# 项目已准备就绪
```

### 3. 启动Claude Code
在`/tmp/weather-cli`目录中启动Claude Code。

### 4. 验证AICS已启用
- 查看调试输出：`[AICS Hook] ... - 检测到.claude目录，自动启用: ...`
- 对话后检查 `.aics/` 目录是否创建

## 测试场景设计

### 场景1: 基础对话流程
**目标**: 验证基本对话保存功能

**测试步骤**:
1. 在Claude Code中询问: "这个天气CLI项目如何工作？"
2. 讨论项目结构: "main.py中的WeatherAPI类有什么作用？"
3. 询问使用方式: "如何运行天气查询命令？"

**预期结果**:
- 项目目录下生成`.aics/`文件夹
- 对话保存到SQLite数据库
- 用户消息和AI响应正确配对

### 场景2: 代码调试和优化
**目标**: 测试技术对话的保存和搜索

**测试步骤**:
1. 报告一个bug: "weather current命令报错：'City not supported'"
2. 请求调试帮助: "如何修复get_current_weather方法中的城市验证？"
3. 讨论优化: "如何改进错误处理，提供更好的错误消息？"
4. 请求重构建议: "如何将WeatherAPI类拆分为更小的模块？"

**预期结果**:
- 技术对话被正确保存
- 错误信息和解决方案可搜索
- 代码片段和讨论被关联保存

### 场景3: 项目结构扩展
**目标**: 测试复杂项目结构的处理能力

**测试步骤**:
1. 讨论新功能: "我想添加天气历史数据查询功能"
2. 设计新模块: "创建history.py模块，应该包含哪些类和方法？"
3. 讨论数据库集成: "是否应该使用SQLite存储历史数据？"
4. 讨论API设计: "REST API端点应该如何设计？"

**预期结果**:
- 复杂架构讨论被完整保存
- 不同话题的对话被正确分组
- 技术决策和理由被记录

### 场景4: 长时间会话测试
**目标**: 测试超时机制和缓冲区管理

**测试步骤**:
1. 开始一个讨论: "让我们设计一个完整的天气数据可视化系统"
2. 等待5分钟（超过默认超时时间）
3. 继续讨论: "可视化应该使用什么图表库？"
4. 讨论具体实现: "如何用matplotlib创建温度趋势图？"

**预期结果**:
- 5分钟超时后，对话被正确分割为两个独立的对话记录
- 缓冲区正确清理过期消息
- 跨超时边界的讨论仍然有意义

### 场景5: 多存储适配器测试
**目标**: 测试不同存储类型的切换

**测试步骤**:
1. 设置环境变量测试SQLite存储: `export AICS_STORAGE_TYPE=sqlite`
2. 讨论: "SQLite存储有什么优势？"
3. 切换到Markdown存储: `export AICS_STORAGE_TYPE=markdown`
4. 讨论: "Markdown格式对于长期存档有什么好处？"
5. 测试Hybrid存储: `export AICS_STORAGE_TYPE=hybrid`
6. 讨论: "混合存储如何结合两者的优点？"

**预期结果**:
- 每种存储类型都能正常工作
- 切换存储类型时不会丢失数据
- 不同存储格式生成正确的文件结构

### 场景6: 搜索功能测试
**目标**: 测试全文搜索和标签提取

**测试步骤**:
1. 进行包含特定关键词的讨论:
   - "使用pandas进行数据分析"
   - "异步请求处理"
   - "单元测试覆盖率"
2. 测试搜索功能:
   - 搜索"pandas"
   - 搜索"异步"
   - 搜索"测试"
3. 检查标签提取:
   - 查看对话的自动生成标签

**预期结果**:
- 关键词搜索返回相关对话
- 技术术语被正确识别和索引
- 自动标签反映对话内容

### 场景7: 项目统计和分析
**目标**: 测试统计功能的使用

**测试步骤**:
1. 进行多个不同类型的对话
2. 询问: "这个项目有多少次关于API设计的讨论？"
3. 检查: "哪些模型被使用过？"
4. 分析: "对话的时间分布如何？"

**预期结果**:
- 项目统计信息准确
- 对话分类和计数正确
- 时间线分析可用

### 场景8: 边界条件测试
**目标**: 测试系统在极端情况下的表现

**测试步骤**:
1. **超长消息测试**: 粘贴一个1000行的代码文件并讨论
2. **特殊字符测试**: 讨论包含Markdown、JSON、XML的内容
3. **并发测试**: 快速连续发送多个消息
4. **错误恢复测试**: 模拟存储失败的情况

**预期结果**:
- 系统处理大消息不崩溃
- 特殊字符被正确转义和保存
- 并发操作正确处理
- 错误被优雅处理

## 自动化测试脚本

我们创建了一个自动化测试脚本`test_aics_boundaries.py`来帮助探索：

```bash
# 运行边界测试
cd /tmp/weather-cli
python test_aics_boundaries.py
```

这个脚本模拟了多种使用场景，包括：
- 不同长度的对话
- 各种技术主题
- 存储适配器切换
- 搜索功能验证

## 预期发现

通过这个探索项目，我们期望发现：

### AICS Hook系统的优势
1. **智能对话分组** - 项目级别的对话组织
2. **多种存储选项** - 适应不同使用场景
3. **强大的搜索** - 全文搜索和标签系统
4. **可靠的状态管理** - 跨进程持久化

### 可能的限制和边界
1. **性能边界** - 大量对话时的响应时间
2. **存储限制** - 大文件的处理能力
3. **并发限制** - 多个会话同时进行时的表现
4. **错误处理边界** - 异常情况的恢复能力

## 数据收集和分析

测试过程中，请记录：

1. **响应时间**: 每个hook的执行时间
2. **存储占用**: 不同存储类型的磁盘使用
3. **搜索准确性**: 搜索结果的相关性
4. **用户体验**: 对话组织的直观性

## 结论目标

通过这个low hanging fruit项目，我们希望：
1. 全面了解AICS Hook系统的能力
2. 识别系统的边界和限制
3. 为未来的优化提供数据支持
4. 验证系统在实际项目中的实用性

**开始探索吧！每个测试场景都是了解系统能力的机会。**